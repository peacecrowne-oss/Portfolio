# Port the Express server listens on.
PORT=3001

# development | production
NODE_ENV=development

# Origin allowed by CORS to call this API (the deployed client's URL in production).
CORS_ORIGIN=http://localhost:5173
