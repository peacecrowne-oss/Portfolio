# Base URL the client's API service layer (src/services/api.ts) will call.
# Leave as "/api" for local dev (proxied to the server by Vite) or set to
# the deployed backend's full origin (e.g. https://api.example.com/api) in production.
VITE_API_BASE_URL=/api
